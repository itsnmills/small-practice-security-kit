# Binder Import Notes

This export contains evidence references and expected evidence items for the companion `hipaa-evidence-binder-template` repo. It does not include PHI, secrets, credentials, or raw evidence files. Store real evidence in approved restricted folders.
