# HIPAA Evidence Binder

This module points to the companion `hipaa-evidence-binder-template` repo. The umbrella kit generates an evidence index that can be copied into the binder or used as a review packet attachment.

The binder should store evidence references, not PHI or secrets.
