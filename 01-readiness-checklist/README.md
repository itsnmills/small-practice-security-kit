# Readiness Checklist

This module captures a plain-English first pass at security readiness: MFA, access reviews, backups, vendors, BAAs, incident contacts, downtime plans, training, and monitoring cadence.

The umbrella builder currently reads these signals from `samples/*.yaml` and generates `readiness-review.md`.
